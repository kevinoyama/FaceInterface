module.exports = {
    presets: [
        "@babel/preset-env", //Alterar as funcionalidades nova do JS que o navegador não entende
        "@babel/preset-react" //Alterar as funcionalidades nova do React que o navegador não entende
    ],
}